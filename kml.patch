diff --git a/src/errors.rs b/src/errors.rs
index 92c1b16..6f228e9 100644
--- a/src/errors.rs
+++ b/src/errors.rs
@@ -30,6 +30,7 @@ pub enum Error {
     InvalidListItemType(String),
     #[error("IO error: {0}")]
     IoError(#[from] std::io::Error),
+    #[cfg(feature = "zip")]
     #[error("ZIP error: {0}")]
     ZipError(#[from] zip::result::ZipError),
     #[error("Invalid units: {0}")]
